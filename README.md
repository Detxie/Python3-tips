#### 日常使用中遇到的与Python相关的知识总结 







##### 1.Python函数是传值还是传引用

答:不是固定的，如果函数传入的是一个可变的对象(如list,dict)，那么就是传的引用，在函数里面修改就是修改的原对象。如果传入的是一个不可变对象(int,float,str,tuple), 那么修改就不是原值，函数传入的只是一个值。这个问题是在做Leetcode题目是遇到的

**Tips**:Python种对象的复制也可以套用上面的逻辑